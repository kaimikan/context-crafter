{
  "name": "context-crafter",
  "version": "1.0.0",
  "description": "A GUI for crafting context prompts for AI assistants.",
  "main": "script.js",
  "scripts": {
    "start": "live-server --port=8080 --no-browser",
    "start-chrome": "live-server --port=8080 --browser=chrome",
    "start-edge": "live-server --port=8080 --browser=edge"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "live-server": "^1.2.2"
  }
}
